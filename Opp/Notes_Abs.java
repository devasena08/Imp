public class Notes_Abs{
    /*
     * abstract class have abstract keyword
     * it have atleast one concrete(non abs) method and abstract method which start with abstrct keyword
     * it means hiding and incomplete
     * abstract method only have method declaration like project abstract means surukam 
     * extend keyword is used
     * Abstraction cant be instantated ie(we cant crete objects)
     * abstract class can have parameterized constructor
     * 
     */
}