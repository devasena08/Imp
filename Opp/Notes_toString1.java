public class Notes_toString1 {
    /*
     * when i give string, integer objects...it prints
     * how it prints 
     * println(when you pass things to be printed..it will take the print statement as )
     * Object class oda object ah eduthukum
     * thn...it will call the valueOf method in String Class...
     * ithu ena panum na....String ah iruntha null nu kudukum
     * ilati toString method ah call panum  toString method in Object class
     * athu ena panum naa..getMethod use pani..package, classs and HashCode ah mtm integer ah print panum
     * 
     * ans:- all the classes String, Integer, Collection classes...elame toString methodfrom base class(Object class)
     * ah override panirku
     * 
     * 
     * So, ipo namlum override pana...hashCode varama print panlam ans ah...
     * 
     * Class Object is the root of the class hierarchy.
     * Every class has Object as a superclass. 
     */
}
