public class Notes_arr {
    /*
     * elements are arrnged in continuous memory
     * except the first  and last, others have two neighbor
     * size is fixed....cant modify
     * static 
     * each partition is indexed
     * length is 0 to n-1
     */
}
