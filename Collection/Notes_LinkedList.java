public class Notes_LinkedList{
    /*
     * arrayList la data retrivel is faster
     * linkedlist la data retrivel is slower
     * linkedlist la data insertion , deletion  is better
     * arrayList la data insertion , deletion takes more shift operations
     * 
     * Has head part and null part
     * each has data and next 
     * head part only have next ,..no previous
     * elements bet head nad null..will have both next nad previous
     * 
     * 
     * Accepts hetrogeneous objects
     * linkedlist has add(offer), remove(poll)
     */
}