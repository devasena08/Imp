public class Notes_ArrayList{
    /*
     * ArrayList is the class which is the used to implement the interface
     * class means it should come under package, it also has constructor ie used 
     * to create object , method,.etc
     * Arraylist is comb of both array and list
     * it is resizable....and length is not used here,...instead size() is used
     * Ds:- Dynamis Array
     * 
     * Util package
     * 
     * 
     * Cons:-
     * ArrayList()---->empty cons irku
     * ----->this has the initial capacity of ten(10 empty box hass been created)
     * ArrayList(Collection<? extends E> c)
     * ArrayList(int initialCapacity)--->we have to specify some initial capacity
     * 
     * 
     * 
     * 
     * ArrayList<Integer> al = new ArrayList<>();
        al.add(10);
        al.add(10);
        al.add(10);
        System.out.println(al);

         No primitive datatypee is allowed in collections
         So, Integer class ku obj ah store akum.
         no need to use for loop to print arrayList..like array

         to check whether it is added or not
         sout....return true or false

         inter:-
         get() and remove() diff
         get()--> only for retrival
         remove()----> for retrival and Deletion ..



         Major methods:-
     *      add
     *      remove(index)
     *      add (any index using index value)
     *      indexOf()---->will return the index value
     *      get--> to retrieve values
     *      set---> to change the value
     *      size()---> to check the size 
     *      contains---> iruka ilaya
     *      addAll--->to copy all
     *      sublist(from index, toindex)-->from index is inclusive, toindex is exclusive
     *      isEmpty
     *      removeAll
     * 
     * 
     * 
     * 
     * Adv:-
     * 
     * Continouus memory
     * data retrievl is faster
     * 
     * 
     * 
     * 
     */
}
